void main() {
  double nota = 8;

  //mensagem recebe retorno: if(nota>=5){aprovado}else{reprovado}
  String mensagem = nota >= 5 ? 'aprovado' : 'reprovado';

  //nota recebe o retorno: if(nota == 5){10}else{2}
  int numero = nota == 5 ? 10 : 2;
}
